# statsum

Tiny numeric toolbox: `bin/statsum` prints the total of its arguments;
`lib/rolling.sh` derives a rolling average on top of the same core.

```sh
bin/statsum 1 2 3        # total=6
bash tests/test-cli.sh   # run the suite
```
